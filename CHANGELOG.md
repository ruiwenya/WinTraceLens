# 更新日志

## Version2.0-preview.2 - 2026-09-02

### 取证采集与导出

- 新增全局“导出取证包”入口，集中保全本次运行中的分析数据，生成包含 JSON、CSV、TXT 摘要、采集清单和 SHA-256 校验值的 ZIP。
- 支持全选或勾选部分证据来源，导出完整字段，不受页面分页、关键词过滤及隐藏列影响。
- 新增“一键获取证据”，按所选来源及必要关联依赖分阶段采集，可设置日志日期范围、结果条数和进程上限，无需逐页操作。
- 支持停止后续采集并保留已完成结果；记录采集失败、权限限制、范围限制及旧快照时间，避免将未采集解释为没有异常。
- 将 Go 原生落地点统一纳入文件痕迹，修正取证范围显示与实际导出内容不一致的问题。

### 进度与数据保全

- 为加载与导出进度增加小号百分比；一键采集按已处理阶段计数，无法精确计量的接口等待明确标注为估算。
- 完善 EvidenceStore 的会话快照保留与并发读取，后台采集期间仍可导出已完成的数据。
- ZIP 提供逐文件 SHA-256 校验，CSV 防公式注入；不导出本工具保存的 API Key、AI 对话或原始样本。

### 使用说明

- 一键采集不自动运行 YARA，也不调用在线 AI。YARA 仅导出已有扫描结果。
- 正在执行的系统查询需要完成或超时收尾后才停止；不同来源不是同一时刻的系统镜像。
- GUI 与 CLI 同步更新，使用 Go 1.26.3 构建。发布文件未进行 Authenticode 签名，请核对 Release 中的 SHA-256 清单，并遵守组织的软件运行策略。
